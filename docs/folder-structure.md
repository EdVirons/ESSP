# Folder structure

- `services/*` — deployable services (each has its own go.mod)
- `shared/` — shared Go packages (http helpers, cursor, ids)
- `deployments/` — docker and k8s
- `docs/` — architecture, RBAC, SSOT rules
