{{- if .Values.ssotSchool.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "essp.fullname" . }}-ssot-school
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "essp.labels" . | nindent 4 }}
    app.kubernetes.io/component: ssot-school
spec:
  type: {{ .Values.ssotSchool.service.type }}
  ports:
  - port: {{ .Values.ssotSchool.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "essp.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: ssot-school
{{- end }}
